import { ScrollView, StyleSheet, Text, View } from "react-native";
import DonarCard from "../ui/donarcard/donarcard";
import SearchComponent from "../home/search";

export default function FindDonars() {
  return (
    <>
      {/* <View style={styles.container}>
        <SearchComponent />
        <ScrollView>
          <DonarCard />
        </ScrollView>
      </View> */}
      <Text>currently we are working on this system will back soon</Text>
    </>
  );
}

const styles = StyleSheet.create({
  container: {},
});
